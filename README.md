# 🏗️ SmartStruct v7.0 Pro — دليل النشر

## 📁 ملفات المشروع

| الملف | الحجم | الوصف |
|-------|-------|-------|
| `index.html` | ~660KB | التطبيق الرئيسي |
| `admin-login.html` | ~21KB | لوحة تحكم المسؤول |
| `supabase-db.js` | ~32KB | محرك قاعدة البيانات السحابية |
| `supabase-schema.sql` | ~16KB | هيكل قاعدة البيانات |

---

## 🚀 النشر السريع (وضع Offline)

ارفع الملفات الأربعة على أي استضافة:
- **Netlify** / **Vercel** / **GitHub Pages** (مجاني)
- أو أي استضافة عادية

يعمل فوراً بدون أي إعداد إضافي. البيانات تُحفظ في `localStorage`.

---

## ☁️ تفعيل Supabase (وضع Cloud)

### الخطوات:

1. **أنشئ مشروع مجاني** على [supabase.com](https://supabase.com)

2. **شغّل الـ Schema**  
   افتح `SQL Editor` في لوحة Supabase وانسخ محتوى `supabase-schema.sql` وشغّله

3. **احصل على المفاتيح**  
   اذهب إلى `Settings → API` وانسخ:
   - `Project URL`
   - `anon public key`

4. **أدخل المفاتيح في التطبيق**  
   في SmartStruct: `الإعدادات → النسخ الاحتياطي` أو `لوحة الإدارة → قاعدة البيانات Supabase`

5. **احفظ واختبر الاتصال** ✅

---

## 🔄 نظام الاتصال الذكي (جديد v7.1)

### المشكل السابق
كان الاتصال ينقطع بعد ساعات ويحتاج إعادة يدوية.

### الحل المطبّق
- **Heartbeat** — ping تلقائي كل 30 ثانية للتأكد من الاتصال
- **Auto-reconnect** — يكتشف الانقطاع ويعيد الاتصال تلقائياً
- **Exponential Backoff** — محاولات متدرجة: 5ث ← 10ث ← 20ث ← ... ← دقيقتين
- **Network Events** — يتفاعل مع انقطاع/عودة الإنترنت فوراً
- **Page Visibility** — عند عودة المستخدم للتبويب يتحقق من الاتصال
- **Queue محمية** — البيانات المدخلة أثناء الانقطاع لا تضيع وتُزامَن عند عودة الاتصال

---

## 🔐 بيانات الدخول

### لوحة الإدارة (`admin-login.html`)
| الحقل | القيمة |
|-------|--------|
| البريد | `admin@smartbtp.dz` |
| كلمة المرور | `Admin@SmartStruct2025` |
| مفتاح الأمان | `SSTR-ADMIN-2025` |

### حساب تجريبي (التطبيق الرئيسي)
| الحقل | القيمة |
|-------|--------|
| البريد | `demo@algerie-construction.dz` |
| كلمة المرور | `Demo@1234` |

---

## ⚙️ البنية التقنية

```
SmartStruct
├── index.html          # SPA كامل (50+ ميزة)
├── admin-login.html    # لوحة إدارة المسؤول
├── supabase-db.js      # طبقة البيانات
│   ├── SUPABASE_CONFIG  # إعدادات الاتصال
│   ├── SupabaseClient   # REST API wrapper
│   ├── DBHybrid         # Supabase + localStorage
│   │   ├── Heartbeat    # مراقبة الاتصال كل 30ث
│   │   ├── Auto-reconnect # إعادة اتصال تلقائي
│   │   └── Sync Queue   # مزامنة محمية
│   └── SupabaseSettings # واجهة الإعدادات
└── supabase-schema.sql # 16 جدول PostgreSQL
```

---

## 🗄️ جداول قاعدة البيانات

| الجدول | الوصف |
|--------|-------|
| `plans` | خطط الاشتراك |
| `tenants` | الشركات المشتركة |
| `users` | المستخدمون |
| `projects` | المشاريع |
| `workers` | العمال |
| `equipment` | المعدات |
| `transactions` | المعاملات المالية |
| `attendance` | الحضور والغياب |
| `materials` | المواد والمخزون |
| `stock_movements` | حركة المخزون |
| `invoices` | الفواتير |
| `salary_records` | سجلات الرواتب |
| `kanban_tasks` | مهام Kanban |
| `documents` | الوثائق |
| `obligations` | الالتزامات |
| `notes` | الملاحظات |

---

## 📞 الدعم

SmartStruct v7.1 Pro — مصمم للشركات الجزائرية في قطاع البناء والمقاولات
